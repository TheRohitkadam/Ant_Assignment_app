import { Card, Typography } from "antd";

const { Title } = Typography;

const AccessTab = () => {
  return (
    <Card style={{ borderRadius: 10 }}>
      <Title level={4}>AccessTab</Title>
    </Card>
  );
};

export default AccessTab;
